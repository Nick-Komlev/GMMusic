<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate x:Key="TrackBrowserDataTemplate" DataType="ListBox.ItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Name}" />
            <TextBlock Text="{Binding Duration}" Grid.Column="1" Padding="10,0"/>
        </Grid>
    </DataTemplate>
</ResourceDictionary>